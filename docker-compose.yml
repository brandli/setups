version: "3.8"

services:
  devcontainer:
    build:
      dockerfile: Dockerfile
    
    volumes:
      - ..:/workspace:cached
      - "~/.gitconfig:/etc/gitconfig"
      
    ports:
      - "3000:3000"

    command: sleep infinity